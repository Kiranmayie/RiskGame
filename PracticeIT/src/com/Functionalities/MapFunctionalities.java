package com.Functionalities;

import java.io.File;

import javafx.stage.FileChooser;

public class MapFunctionalities {

	public static void saveMap(File file, com.units.Map map) throws Exception {
		/*MapValidate.validateMap(map);
		MapFileWriter fileWriter = new MapFileWriter();

		if (file == null) {
			FileChooser fileChooser = new FileChooser();

			// Set extension filter
			FileChooser.ExtensionFilter extFilter = new FileChooser.ExtensionFilter("Map files (*.map)", "*.map");
			fileChooser.getExtensionFilters().add(extFilter);

			// Show save file dialog
			file = fileChooser.showSaveDialog(null);
		}

		fileWriter.writeMapToFile(map, file);
	}*/

	}}
